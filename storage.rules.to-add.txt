LEGACY / NO CANÓNICO

No uses este archivo como reglas completas ni lo pegues sin comparar con las reglas ya desplegadas.

Fuente canónica local del proyecto: storage.rules
Configuración que apunta a esa fuente: firebase.json -> storage.rules
Estado: no desplegado por esta auditoría.

Importante:
- Las fotos de perfil actuales se suben a Cloudinary desde src/services/photoUploadService.js; no dependen de profile_photos en Firebase Storage.
- Las fotos del chat público usan Firebase Storage en chat_media/rooms/{ownerUid}/{roomId}/{messageId}/{assetPath=**}.
- Las fotos del chat privado activo usan Firebase Storage en chat_media/private/{ownerUid}/{chatId}/{messageId}/{assetPath=**}.
- La ruta y los límites de esos flujos están definidos únicamente en storage.rules y deben probarse con Emulator Suite antes de desplegar.
- No afirmes que SQL de Supabase habilita estos uploads: requeriría bucket, políticas de Storage, adapter y metadatos de mensaje.

Si el proyecto Firebase ya tiene reglas adicionales, no reemplaces el archivo a ciegas. Haz una comparación manual, prueba las reglas resultantes en el emulador y despliega solo después de una autorización específica.
